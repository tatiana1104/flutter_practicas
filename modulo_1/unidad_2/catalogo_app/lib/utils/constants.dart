import 'package:flutter/material.dart';

/// Colores de la aplicación
/// Color primario usado para elementos principales como botones
const Color primaryColor = Color(0xFF4A90E2);

/// Color secundario para acentos y elementos destacados
const Color secondaryColor = Color(0xFFD0011B);

/// Gris oscuro para texto principal
const Color grayDark = Color(0xFF4A4A4A);

/// Gris claro para fondos secundarios
const Color grayLight = Color(0xFFDDDDDD);

/// Color de fondo general de la aplicación
const Color backgroundColor = Color(0xFFFAFAFA);

/// Color blanco para elementos de UI
const Color whiteColor = Colors.white;

/// Tipografía (Fuente: Roboto)
/// Estilo de texto para el cuerpo del contenido
const TextStyle bodyTextStyle = TextStyle(
  fontFamily: 'Roboto',
  fontSize: 14,
  fontWeight: FontWeight.w400,
);

/// Estilo de texto para subtítulos
const TextStyle subtitleTextStyle = TextStyle(
  fontFamily: 'Roboto',
  fontSize: 16,
  fontWeight: FontWeight.w500,
);

/// Estilo de texto para títulos
const TextStyle titleTextStyle = TextStyle(
  fontFamily: 'Roboto',
  fontSize: 20,
  fontWeight: FontWeight.w500,
);

/// Estilo de texto para encabezados
const TextStyle headingTextStyle = TextStyle(
  fontFamily: 'Roboto',
  fontSize: 28,
  fontWeight: FontWeight.w700,
);

/// Espacios (cuadrícula de 8 puntos)
/// Espaciado pequeño
const double smallSpacing = 8.0;

/// Espaciado mediano
const double mediumSpacing = 16.0;

/// Espaciado grande
const double largeSpacing = 24.0;

/// Espaciado extra grande
const double extraLargeSpacing = 32.0;

/// Dimensiones del logo
/// Tamaño del logo circular en la pantalla de login
const double logoSize = 96.0;

/// Radios de borde
/// Radio de borde para los campos de entrada
const double inputBorderRadius = 8.0;

/// Radio de borde para el botón principal
const double buttonBorderRadius = 12.0;

/// Colores adicionales
/// Color del texto del título principal
const Color titleColor = Color(0xFF111111);

/// Color del borde de los campos de entrada
const Color inputBorderColor = Color(0xFFDDDDDD);

/// Padding interno
/// Padding izquierdo para el contenido de los inputs
const double inputPaddingLeft = 12.0;

/// Alturas
/// Altura estándar de los campos de entrada
const double inputHeight = 48.0;

/// Altura del botón de acción principal
const double buttonHeight = 48.0;

/// Ancho del borde de contenedores decorativos
const double borderWidth = 2.0;
